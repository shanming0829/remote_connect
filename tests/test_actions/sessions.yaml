config:
    log:
        level: DEBUG
        console: True
        dir: logs
        file: runner.log
    session:
        crlf: "\n"
        timeout: 10

sessions:

    ssh_session:
        hostname: localhost
        protocol: ssh
        username: test
        password: test